apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysubapp.fullname" . }}
  labels:
    {{- include "mysubapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysubapp.selectorLabels" . | nindent 4 }}
