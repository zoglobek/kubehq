apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysubapp.fullname" . }}-test-connection"
  labels:
    {{- include "mysubapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysubapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
