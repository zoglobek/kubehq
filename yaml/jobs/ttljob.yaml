apiVersion: batch/v1
kind: Job
metadata:
  name: hello-job
spec:
  ttlSecondsAfterFinished: 60  # deleted 60 seconds after completing
  template:
    spec:
      containers:
      - name: hello
        image: busybox
        command: ["echo", "Hello from Job!"]
      restartPolicy: Never
